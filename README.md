# AETHER TYPE

Current playable build: `typer.html`

Author metadata in the game: `Dr BennyT`

Suggested next growth path:

- keep `typer.html` as the playable single-file prototype
- add `docs/` for design notes and tuning logs
- add `assets/` when the game starts using external audio or art
- add `src/` and `dist/` only when you are ready to split and bundle systems

For now, this folder gives the game its own home on the Desktop without forcing a rewrite away from the single-file workflow.
